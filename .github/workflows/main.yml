name: Automated Shorts Generator Pipeline

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch: # Allows you to click "Run Workflow" manually from GitHub UI

jobs:
  build-and-generate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

     Stein: Set up Python 3.10
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install System Dependencies (FFmpeg)
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run Automation Pipeline
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      run: |
        python main.py

    # 📥 EXTRACTION STEP: Saves the file to GitHub before the server vanishes
    - name: Upload Final Video Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MrBeast-Style-Short-Video
        path: assets/final/final_short.mp4
        retention-days: 5 # Saves space; keeps the download link active for 5 days
